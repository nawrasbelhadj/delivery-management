{% extends 'base.html.twig' %}
{% block stylesheets %}
{% endblock %}

{% block body %}
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Corrier</h4>
                    <div class="table-responsive pt-3">
                    {% if courrier is null%}
                        <p>Courrier not fouind</p>
                    {% else %}
                        <p>{{courrier.id}}</p>
                        <p>{{courrier.codeBarre}}</p>
                        <p>{{courrier.postDeparture}}</p>
                    {% endif %}
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}